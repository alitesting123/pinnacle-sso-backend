option_settings:
  aws:elasticbeanstalk:command:
    Timeout: 1800  # Increase timeout to 30 minutes for ML package installation
  aws:elasticbeanstalk:application:environment:
    PIP_DEFAULT_TIMEOUT: "300"
    PIP_NO_CACHE_DIR: "1"  # Disable pip cache to avoid disk space issues
